# 作業三指令速查表

快速查找常用指令和操作步驟。

---

## 🚀 快速開始 (3 步驟)

```bash
# 1. 執行設定腳本
bash setup_project.sh

# 2. 建立原始碼檔案 (參考教學指南複製貼上)
# - src/preprocessing.py
# - src/model.py  
# - src/train.py
# - src/app.py

# 3. 訓練模型
cd src && python train.py

# 4. 測試應用
streamlit run app.py
```

---

## 📦 環境設定

### Python 虛擬環境

```bash
# 建立虛擬環境
python3 -m venv venv

# 啟動虛擬環境 (macOS/Linux)
source venv/bin/activate

# 啟動虛擬環境 (Windows)
venv\Scripts\activate

# 停用虛擬環境
deactivate

# 檢查虛擬環境
which python  # 應該指向 venv/bin/python
```

### 套件管理

```bash
# 安裝所有依賴
pip install -r requirements.txt

# 安裝單一套件
pip install streamlit

# 查看已安裝套件
pip list

# 匯出套件清單
pip freeze > requirements.txt

# 升級 pip
pip install --upgrade pip
```

---

## 🔧 OpenSpec 指令

### 查看規格

```bash
# 列出所有規格
openspec spec list

# 詳細列表
openspec spec list --long

# 查看變更歷史
openspec list

# 查看特定規格
openspec spec show spam-detection
```

### 建立變更提案

```bash
# 使用 AI 助手建立
# 在對話中說: "幫我建立變更提案，要新增資料視覺化功能"

# 驗證提案
openspec validate add-visualization --strict

# 驗證所有提案
openspec validate --all
```

### 應用變更

```bash
# 應用變更到主規格
openspec apply add-visualization

# 歸檔完成的變更
openspec archive add-visualization
```

---

## 🤖 模型訓練

### 基本訓練

```bash
# 進入 src 目錄
cd src

# 執行訓練
python train.py

# 查看輸出
# 應該看到準確率、精確率等指標
```

### Python 互動式訓練

```python
# 啟動 Python
python3

# 載入模組
from model import SpamClassifier

# 建立分類器
classifier = SpamClassifier()

# 載入資料
df = classifier.load_data('../data/sms_spam_no_header.csv')
print(f"資料筆數: {len(df)}")

# 準備資料
df = classifier.prepare_data(df)

# 分割資料
from sklearn.model_selection import train_test_split
X_train, X_test, y_train, y_test = train_test_split(
    df['processed_message'], df['label'], test_size=0.2, random_state=42
)

# 訓練
classifier.train(X_train, y_train)

# 評估
metrics = classifier.evaluate(X_test, y_test)
print(metrics)

# 儲存
classifier.save_model()

# 測試預測
result = classifier.predict("Congratulations! You won $1000!")
print(result)
```

---

## 🌐 Streamlit 應用

### 本地執行

```bash
# 基本執行
streamlit run app.py

# 指定端口
streamlit run app.py --server.port 8080

# 不自動開啟瀏覽器
streamlit run app.py --server.headless true

# 開發模式 (自動重載)
streamlit run app.py --server.runOnSave true
```

### 查看 Streamlit 資訊

```bash
# 版本資訊
streamlit version

# 設定說明
streamlit config show

# 清除快取
streamlit cache clear
```

---

## 📊 資料探索

### 使用 Pandas

```python
import pandas as pd

# 載入資料
df = pd.read_csv('data/sms_spam_no_header.csv', sep='\t', names=['label', 'message'])

# 基本資訊
print(df.head())
print(df.info())
print(df.describe())

# 標籤分布
print(df['label'].value_counts())
print(df['label'].value_counts(normalize=True))

# 郵件長度分析
df['length'] = df['message'].str.len()
print(df.groupby('label')['length'].describe())

# 常見詞彙
from collections import Counter
spam_words = ' '.join(df[df['label']=='spam']['message']).split()
print(Counter(spam_words).most_common(10))
```

### 快速統計

```bash
# 使用 awk 統計行數
wc -l data/sms_spam_no_header.csv

# 查看前 10 行
head -10 data/sms_spam_no_header.csv

# 查看後 10 行
tail -10 data/sms_spam_no_header.csv

# 統計 spam/ham 數量
cut -f1 data/sms_spam_no_header.csv | sort | uniq -c
```

---

## 🧪 測試與驗證

### 單元測試

```bash
# 執行所有測試
pytest

# 執行特定測試檔案
pytest tests/test_preprocessing.py

# 顯示詳細輸出
pytest -v

# 顯示 print 輸出
pytest -s

# 測試覆蓋率
pytest --cov=src
```

### 語法檢查

```bash
# 檢查語法錯誤
python -m py_compile src/model.py

# 使用 flake8 檢查風格
flake8 src/

# 使用 black 格式化
black src/

# 使用 pylint 檢查
pylint src/model.py
```

### 手動測試

```python
# 測試前處理
from preprocessing import TextPreprocessor
prep = TextPreprocessor()

test_cases = [
    "WINNER!! Call NOW!!!",
    "Hi John, meeting at 3pm?",
    "Free delivery today only!!!"
]

for text in test_cases:
    result = prep.preprocess(text)
    print(f"原始: {text}")
    print(f"處理: {result}\n")
```

---

## 📁 Git 版本控制

### 基本操作

```bash
# 初始化
git init

# 查看狀態
git status

# 加入檔案
git add .
git add src/model.py  # 加入特定檔案

# 提交
git commit -m "feat: Add spam classifier model"

# 查看歷史
git log --oneline

# 查看差異
git diff
```

### 遠端操作

```bash
# 加入遠端
git remote add origin https://github.com/username/repo.git

# 查看遠端
git remote -v

# 推送到遠端
git push -u origin main
git push  # 後續推送

# 從遠端拉取
git pull origin main

# 複製專案
git clone https://github.com/username/repo.git
```

### 分支操作

```bash
# 建立分支
git branch feature-visualization

# 切換分支
git checkout feature-visualization

# 建立並切換
git checkout -b feature-visualization

# 查看分支
git branch

# 合併分支
git checkout main
git merge feature-visualization

# 刪除分支
git branch -d feature-visualization
```

### 提交訊息規範

```bash
# 功能
git commit -m "feat: Add data visualization"

# 修復
git commit -m "fix: Fix model loading error"

# 文件
git commit -m "docs: Update README"

# 樣式
git commit -m "style: Format code with black"

# 重構
git commit -m "refactor: Improve preprocessing logic"

# 測試
git commit -m "test: Add unit tests"

# 雜項
git commit -m "chore: Update dependencies"
```

---

## 🌐 Streamlit Cloud 部署

### 部署準備

```bash
# 1. 確認檔案齊全
ls -la src/app.py
ls -la requirements.txt
ls -la data/sms_spam_no_header.csv

# 2. 推送到 GitHub
git add .
git commit -m "deploy: Prepare for Streamlit Cloud"
git push origin main

# 3. 前往 Streamlit Cloud
# https://streamlit.io/cloud
```

### 部署設定

```yaml
# Streamlit 設定檔 .streamlit/config.toml
[server]
port = 8501
headless = true

[browser]
gatherUsageStats = false
```

### 檢查部署狀態

```bash
# 查看部署日誌 (在 Streamlit Cloud 網站)
# Manage app > Logs

# 常見問題
# 1. ModuleNotFoundError -> 檢查 requirements.txt
# 2. FileNotFoundError -> 檢查檔案路徑
# 3. Memory error -> 減少模型大小
```

---

## 🐛 常見問題解決

### NLTK 資料下載

```python
# 方法 1: 在程式中下載
import nltk
nltk.download('stopwords')
nltk.download('punkt')

# 方法 2: 使用 Python 命令
python -m nltk.downloader stopwords
python -m nltk.downloader punkt

# 方法 3: 下載全部
python -m nltk.downloader all
```

### 模型載入錯誤

```python
# 檢查模型檔案是否存在
import os
print(os.path.exists('model.pkl'))
print(os.path.exists('vectorizer.pkl'))

# 檢查當前目錄
print(os.getcwd())

# 使用絕對路徑
import joblib
model = joblib.load('/absolute/path/to/model.pkl')
```

### Streamlit 錯誤排除

```bash
# 清除快取
streamlit cache clear

# 重新啟動
pkill -f streamlit
streamlit run app.py

# 檢查端口占用
lsof -i :8501

# 使用其他端口
streamlit run app.py --server.port 8080
```

### 依賴衝突

```bash
# 檢查依賴
pip check

# 更新特定套件
pip install --upgrade scikit-learn

# 重新安裝所有套件
pip uninstall -r requirements.txt -y
pip install -r requirements.txt

# 使用虛擬環境重新開始
deactivate
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

---

## 📈 效能檢查

### 模型評估

```python
from sklearn.metrics import classification_report, confusion_matrix

# 詳細報告
print(classification_report(y_test, y_pred))

# 混淆矩陣
print(confusion_matrix(y_test, y_pred))

# ROC 曲線
from sklearn.metrics import roc_curve, auc
import matplotlib.pyplot as plt

fpr, tpr, _ = roc_curve(y_test, y_pred_proba)
roc_auc = auc(fpr, tpr)
plt.plot(fpr, tpr, label=f'AUC = {roc_auc:.2f}')
plt.show()
```

### 執行時間測試

```python
import time

# 測試預測速度
start = time.time()
for _ in range(100):
    classifier.predict("Test message")
end = time.time()
print(f"平均預測時間: {(end-start)/100*1000:.2f} ms")
```

---

## 🔍 除錯技巧

### Python 除錯

```python
# 使用 print
print(f"變數值: {variable}")

# 使用 pdb
import pdb; pdb.set_trace()

# 使用 IPython
from IPython import embed; embed()

# 記錄錯誤
import logging
logging.basicConfig(level=logging.DEBUG)
logging.debug("除錯訊息")
```

### 檢查資料流

```python
# 在每個步驟印出資料
print("1. 載入資料:", df.shape)
print("2. 前處理後:", df_processed.shape)
print("3. 向量化後:", X_tfidf.shape)
print("4. 預測結果:", y_pred.shape)
```

---

## 📚 進階操作

### 模型優化

```python
# 網格搜尋
from sklearn.model_selection import GridSearchCV

params = {
    'alpha': [0.1, 0.5, 1.0],
    'fit_prior': [True, False]
}

grid = GridSearchCV(MultinomialNB(), params, cv=5)
grid.fit(X_train, y_train)
print(f"最佳參數: {grid.best_params_}")
```

### 匯出資料

```python
# 匯出預測結果
results = pd.DataFrame({
    'message': X_test,
    'actual': y_test,
    'predicted': y_pred
})
results.to_csv('results.csv', index=False)

# 匯出模型指標
metrics_df = pd.DataFrame([metrics])
metrics_df.to_csv('metrics.csv', index=False)
```

---

## 🎯 快速檢查清單

```bash
# 環境檢查
python3 --version          # ✓ Python 3.8+
git --version              # ✓ Git installed
openspec --version         # ✓ OpenSpec CLI

# 檔案檢查
ls src/preprocessing.py    # ✓ 前處理模組
ls src/model.py           # ✓ 模型模組
ls src/train.py           # ✓ 訓練腳本
ls src/app.py             # ✓ Streamlit 應用

# 訓練檢查
python src/train.py        # ✓ 訓練成功
ls model.pkl              # ✓ 模型已儲存
ls vectorizer.pkl         # ✓ 向量器已儲存

# 應用檢查
streamlit run src/app.py   # ✓ 應用啟動
# 訪問 http://localhost:8501  # ✓ 網頁正常

# Git 檢查
git status                 # ✓ 無未追蹤檔案
git remote -v             # ✓ 遠端已設定
git log --oneline          # ✓ 有提交記錄
```

---

## 📞 獲取幫助

```bash
# Python 幫助
python --help

# Streamlit 幫助
streamlit --help
streamlit run --help

# Git 幫助
git --help
git commit --help

# OpenSpec 幫助
openspec --help
openspec validate --help

# 查看套件文件
python -m pydoc sklearn.naive_bayes.MultinomialNB
```

---

**提示**: 將此檔案加入書籤，作為快速參考！

**更新日期**: 2025-11-10
